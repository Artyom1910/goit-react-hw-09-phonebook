import styles from './pages.module.scss';

const HomePage = () => (
  <div className={styles.HomePage}>
    <h1 className={styles.title}>Phonebook contacts app</h1>
  </div>
);
export default HomePage;
